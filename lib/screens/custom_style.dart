import 'package:flutter/material.dart';
import 'color_selection.dart';

class CustomStyle extends StatefulWidget {
  const CustomStyle({Key? key}) : super(key: key);

  @override
  _CustomStyleState createState() => _CustomStyleState();
}

class _CustomStyleState extends State<CustomStyle> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
