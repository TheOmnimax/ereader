import 'package:flutter/material.dart';

class DownloadEbook extends StatelessWidget {
  const DownloadEbook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
