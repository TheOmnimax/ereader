import 'package:flutter/material.dart';

// Currently only has the processed content, but will update later with IDs and such. Adding here now for later compatibility.
class PageData {
  const PageData({
    required this.content,
  });

  final TextSpan content;
}
